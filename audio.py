class Audio:
    def __init__(self,title,artist,length,size,file_format='mp3'):
            self.title = title
            self.artist = artist 
            self.length = length
            self.size = size
            self.file_format = file_format